# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables
.env
.env.local
.env.*.local

# Storybook build output
storybook-static

# Playwright E2E
test-results
playwright-report
/playwright/.cache

# Test coverage (bun test --coverage-dir=coverage / deno test --coverage=coverage/deno /
# storybook test-runner --coverageDirectory coverage/storybook)
coverage

# VRT: jest-image-snapshot が差分検出時に diff/actual 画像を書き出す先。
# baseline（__image_snapshots__/*.png）はコミットするが、この出力は生成物なので追跡しない
# （CI では _a11y.yml の Upload visual regression diffs が artifact として保存する）
.storybook/__image_snapshots__/__diff_output__/

# Supabase CLI temp files
supabase/.temp/

# Stryker mutation testing reports/temp
reports/

# Serena MCP local metadata
.serena/

# Claude Code agent worktrees (local orchestration state)
.claude/worktrees/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
