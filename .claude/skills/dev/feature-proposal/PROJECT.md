# PROJECT.md — feature-proposal（housekeeper 固有設定）

このファイルは housekeeper リポジトリ専用の設定。SKILL.md の一般論より **こちらを優先** する。
他リポジトリへ持ち出す場合はこのファイルを削除するか、移植先の値で書き直すこと。

## プロダクト制約（提案の前提条件）

- セルフホスト・**単一ユーザー**の家庭内在庫管理。公開機能・マルチテナントは対象外
- **バックエンドサーバーなし**。Supabase クライアント直 + RLS。
  外部 API が必要なら Supabase Edge Functions（例: barcode-lookup）
- モバイルファースト。キッチン・買い物中の片手操作が主
- 価値の軸: **食品ロス削減・買い忘れ/二重購入の防止・記録の手間最小化**

この制約に反する提案（例: 家族間共有のリアルタイム同期サーバー）は、
制約内に収まる代案に変形するか、提案しない。

## 文書構成

| 文書                       | 役割                                                      |
| -------------------------- | --------------------------------------------------------- |
| `PLANS.md`                 | ロードマップ + TODO の単一の真実の源。TODO は §10 配下    |
| `docs/specs/overview.md`   | プロダクト概要                                            |
| `docs/specs/database.md`   | DB スキーマ（テーブル・削除動作・RLS）                    |
| `docs/specs/features/*.md` | 機能別 spec。新規 spec の見出し構成はここの既存に合わせる |
| `CLAUDE.md` の Specs 一覧  | spec 索引。新規 spec を作ったら 1 行追加する              |

## Phase 体系

v1 (MVP) → v1.1 → v1.2 (PWA/通知) → v1.3 (統計) → Backlog。
優先度・時期の提案はこの体系で書く。

## spec ドラフトの見出し構成（既存 spec 踏襲）

```markdown
# Feature Spec: <名前>

## 概要

## スコープ判断

## ユーザーストーリー

## 実装方針

## データ

## エラー

## <phase> 範囲

## Backlog
```

- 実装方針にはコンポーネント配置（Atomic Design 層）と hooks の置き場所まで書く
- データ節は `docs/specs/database.md` の書式（削除動作の表など）に合わせる

## ロードマップ反映のルール

- `PLANS.md` §10 の該当 Phase 見出し配下に `- [ ] タスク` を追記する
- **`<!-- issue:#NN -->` は付けない**。Issue 起票は `issue-sync` スキルの責務
  （追記後に「Issue 化する？」と提案するのはよい）
- 重要な判断は PLANS.md の決定ログにも追記する
